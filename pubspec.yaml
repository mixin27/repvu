name: repvu
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  # oauth2 for github oauth2 authentication
  oauth2: ^2.0.0
  # storage
  flutter_secure_storage: ^4.2.1
  # functional programming
  dartz: ^0.10.0
  # networking
  dio: ^4.0.0
  # state management
  flutter_hooks: ^0.18.0
  hooks_riverpod: ^0.14.0+5
  # data class
  freezed_annotation: ^0.14.3
  # webview for markdown
  webview_flutter: ^2.1.1
  # toast message
  flash: ^2.0.3
  # navigation route
  auto_route: ^3.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.1.4
  # flutter_lints: ^1.0.0
  freezed: ^0.14.5
  auto_route_generator: ^3.0.1
  lint: ^1.7.2

flutter:
  uses-material-design: true

